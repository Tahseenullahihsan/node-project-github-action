name: workflow for the nodejs application

on: 
  push: 
    branches:
      - 'feature/**'
  pull_request:
    branches: 
      - 'feature/**'

jobs:
  ci-cd:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: .
